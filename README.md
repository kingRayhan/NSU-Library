# NSU LIBRARY

A simple library management system built with JavaFX and JDBC.

## Run with Gradle

From `/home/runner/work/NSU-Library/NSU-Library`:

```bash
./gradlew run
```

## Build with Gradle

```bash
./gradlew clean build
```

## Notes

- Main class: `info.rayhan.nsu_library.App`
- Source and resources are both under `src/`
- SQLite database file is `src/database/library.db`
